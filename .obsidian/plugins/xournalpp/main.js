/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => XoppPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian14 = require("obsidian");

// src/XoppSettingsTab.ts
var import_obsidian4 = require("obsidian");

// src/modals/ConfirmationModal.ts
var import_obsidian = require("obsidian");
var ConfirmationModal = class extends import_obsidian.Modal {
  constructor(app, onConfirm, onReject, initialValue, toggle) {
    super(app);
    this.confirmed = false;
    this.onConfirm = onConfirm;
    this.onReject = onReject;
    this.initialValue = initialValue;
    this.toggle = toggle;
  }
  onOpen() {
    super.onOpen();
    const { contentEl } = this;
    contentEl.createEl("h2", { text: "Enable Auto Export?" });
    contentEl.createEl("p", {
      text: "Enabling auto export might automatically overwrite any same-named files. Do you want to proceed?"
    });
    const buttonContainer = contentEl.createDiv({ cls: "modal-button-container" });
    const confirmButton = buttonContainer.createEl("button", { text: "Yes" });
    confirmButton.addEventListener("click", async () => {
      this.confirmed = true;
      await this.onConfirm();
      this.close();
    });
    const cancelButton = buttonContainer.createEl("button", { text: "No" });
    cancelButton.addEventListener("click", () => {
      this.onReject();
      this.close();
    });
  }
  onClose() {
    super.onClose();
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/xopp2pdf.ts
var import_obsidian3 = require("obsidian");
var import_child_process2 = require("child_process");

// src/checks.ts
var import_obsidian2 = require("obsidian");
var import_child_process = require("child_process");
async function checkXoppSetup(plugin) {
  let errors = [];
  let userPath = plugin.settings.xournalppPath;
  let aliasPath = "xournalpp";
  let windowsPath = '"c:/Program Files/Xournal++/bin/xournalpp.exe"';
  let macPath = '"/Applications/Xournal++.app/Contents/MacOS/xournalpp"';
  let versionCmd = " --version";
  if (userPath) {
    try {
      await executeCommand(userPath + versionCmd);
      return userPath;
    } catch (error) {
      errors.push("User defined Xournal++ path not working: " + error.message);
    }
  }
  try {
    await executeCommand(aliasPath + versionCmd);
    return aliasPath;
  } catch (error) {
    errors.push(error.message);
  }
  if (import_obsidian2.Platform.isWin) {
    try {
      await executeCommand(windowsPath + versionCmd);
      return windowsPath;
    } catch (error) {
      errors.push(error.message);
    }
  }
  if (import_obsidian2.Platform.isMacOS) {
    try {
      await executeCommand(macPath + versionCmd);
      return macPath;
    } catch (error) {
      errors.push(error.message);
    }
  }
  new import_obsidian2.Notice("Error: Xournal++ path not setup correctly. Please check docs on how to set it up.", 1e4);
  errors.forEach((error) => console.error("Xournal++ Error:" + error));
  return "error";
}
function executeCommand(command) {
  return new Promise((resolve, reject) => {
    (0, import_child_process.exec)(command, (error) => {
      if (error) {
        reject(error);
      } else {
        resolve();
      }
    });
  });
}

// src/xopp2pdf.ts
function exportXoppToPDF(plugin, filePaths, notify = true) {
  let errors = false;
  filePaths.forEach(async (filePath) => {
    let fs = plugin.app.vault.adapter;
    if (fs instanceof import_obsidian3.FileSystemAdapter) {
      let vaultPath = fs.getBasePath();
      let xoppFilePath = vaultPath + "/" + filePath;
      let pdfFilePath = xoppFilePath.replace(".xopp", ".pdf");
      let path = await checkXoppSetup(plugin);
      if (!path || path === "error") {
        new import_obsidian3.Notice("Error: Xournal++ path not setup correctly. Please check docs on how to set it up.", 1e4);
        return;
      }
      let command = `${path} --create-pdf="${pdfFilePath}" "${xoppFilePath}"`;
      (0, import_child_process2.exec)(command, (error) => {
        if (error) {
          console.error(`Error converting Xournal++ to PDF: ${error.message}`);
          errors = true;
          return;
        }
      });
    }
  });
  if (errors) {
    new import_obsidian3.Notice("Error converting Xournal++ to PDF. Check the console for more information.");
    return;
  }
  if (notify)
    new import_obsidian3.Notice("Exported all Xournal++ notes successfully.");
}
function exportAllXoppToPDF(plugin) {
  let files = plugin.app.vault.getFiles();
  files = files.filter((file) => file.extension === "xopp");
  let filePaths = files.map((file) => file.path);
  exportXoppToPDF(plugin, filePaths);
}

// src/XoppSettingsTab.ts
var XoppSettingsTab = class extends import_obsidian4.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    let { containerEl } = this;
    containerEl.empty();
    new import_obsidian4.Setting(containerEl).setName("Auto export Xournal++ files").setDesc("Automatically export Xournal++ files to PDF upon modification.").addToggle((toggle) => {
      toggle.setValue(this.plugin.settings.autoExport).onChange(async (value) => {
        if (value) {
          const initialValue = this.plugin.settings.autoExport;
          const confirmationModal = new ConfirmationModal(this.app, async () => {
            this.plugin.settings.autoExport = true;
            await this.plugin.saveSettings();
            toggle.setValue(true);
            exportAllXoppToPDF(this.plugin);
          }, async () => {
            this.plugin.settings.autoExport = false;
            await this.plugin.saveSettings();
            toggle.setValue(false);
          }, initialValue, toggle);
          confirmationModal.onClose = () => {
            if (!confirmationModal.confirmed) {
              toggle.setValue(false);
            }
          };
          confirmationModal.open();
        } else {
          this.plugin.settings.autoExport = false;
          await this.plugin.saveSettings();
          toggle.setValue(false);
        }
      });
    });
    new import_obsidian4.Setting(containerEl).setName("Xournal++ installation path").setDesc("The path where Xournal++ is installed (leave empty for system default).").addText((toggle) => {
      toggle.setValue(this.plugin.settings.xournalppPath).onChange(async (value) => {
        this.plugin.settings.xournalppPath = value;
        await this.plugin.saveSettings();
      });
    });
    new import_obsidian4.Setting(containerEl).setName("Xournal++ template path").setDesc(
      "The relative path of the template for any new Xournal++ file (leave empty to use the default template)."
    ).addText((toggle) => {
      toggle.setValue(this.plugin.settings.templatePath).setPlaceholder("e.g. templates/template.xopp").onChange(async (value) => {
        this.plugin.settings.templatePath = value;
        await this.plugin.saveSettings();
      });
    });
    new import_obsidian4.Setting(containerEl).setName("Default path for new Xournal++ files").setDesc(
      "The relative path for new Xournal++ files. This folder will be used unless a full path is specified during file creation (leave empty to use root folder)."
    ).addText((toggle) => {
      toggle.setValue(this.plugin.settings.defaultNewFilePath).setPlaceholder("e.g. Notes").onChange(async (value) => {
        this.plugin.settings.defaultNewFilePath = value;
        await this.plugin.saveSettings();
      });
    });
  }
};

// src/xoppActions.ts
var import_obsidian5 = require("obsidian");

// src/template.ts
var base64Template = "";

// src/xoppActions.ts
var import_child_process3 = require("child_process");
async function openXournalppFile(xoppFile, plugin) {
  let path = await checkXoppSetup(plugin);
  let fs = plugin.app.vault.adapter;
  if (fs instanceof import_obsidian5.FileSystemAdapter) {
    let vaultPath = fs.getBasePath();
    if (!path || path === "error") {
      new import_obsidian5.Notice("Error: Xournal++ path not setup correctly. Please check docs on how to set it up.", 1e4);
      return;
    }
    let command = `${path} "${vaultPath + "/" + xoppFile.path}"`;
    new import_obsidian5.Notice("Opening file in Xournal++");
    (0, import_child_process3.exec)(command, (error) => {
      if (error) {
        new import_obsidian5.Notice("Error opening file in Xournal++. Check console for error message.");
        console.error(`Error opening file in Xournal++: ${error.message}`);
        return;
      }
    });
  } else {
    new import_obsidian5.Notice("Error opening file in Xournal++. Check console for error message.");
  }
}
async function createXoppFile(plugin, newNoteName) {
  let newNotePath = newNoteName.startsWith("/") ? newNoteName.slice(1) : newNoteName;
  const fs = plugin.app.vault.adapter;
  try {
    const templatePath = await getTemplateFilePath(plugin, fs);
    await fs.copy(templatePath, newNotePath);
    new import_obsidian5.Notice("Xournal++ note created");
  } catch (e) {
    new import_obsidian5.Notice("Error: Could not create a Xournal++ note: " + e.message);
  }
}
function findCorrespondingXoppToPdf(pdfFilePath, plugin) {
  var _a;
  let xoppFilePath = pdfFilePath == null ? void 0 : pdfFilePath.replace(".pdf", ".xopp");
  let xoppFilename = xoppFilePath.substring(xoppFilePath.lastIndexOf("/") + 1);
  const pdfFile = plugin.app.vault.getFileByPath(pdfFilePath);
  const parentFolder = (_a = pdfFile == null ? void 0 : pdfFile.parent) != null ? _a : plugin.app.vault.getFolderByPath("/");
  const xoppFile = parentFolder == null ? void 0 : parentFolder.children.find((child) => child.name === xoppFilename);
  if (xoppFile instanceof import_obsidian5.TFile)
    return xoppFile;
}
async function getTemplateFilePath(plugin, fs) {
  const userTemplatePath = plugin.settings.templatePath;
  if (userTemplatePath) {
    if (!await fs.exists(userTemplatePath))
      throw new Error("Could not find the given template file.");
    return userTemplatePath;
  }
  const DEFAULT_TEMPLATE_PATH = plugin.app.vault.configDir + "/plugins/" + plugin.manifest.id + "/template.xopp";
  if (!await fs.exists(DEFAULT_TEMPLATE_PATH)) {
    await createTemplate(plugin, DEFAULT_TEMPLATE_PATH).catch(() => {
      throw new Error("Unable to get or create the default template.");
    });
  }
  return DEFAULT_TEMPLATE_PATH;
}
async function createTemplate(plugin, path) {
  let binaryString = atob(base64Template);
  let bytes = new Uint8Array(binaryString.length);
  for (let i = 0; i < binaryString.length; i++) {
    bytes[i] = binaryString.charCodeAt(i);
  }
  await plugin.app.vault.createBinary(path, bytes.buffer);
}
function renameXoppFile(plugin, xoppFile, pdfFile, fileName) {
  let filePath = xoppFile.path.split("/");
  filePath.pop();
  let newPath = (filePath == null ? void 0 : filePath.join("/")) + "/" + fileName;
  plugin.app.fileManager.renameFile(xoppFile, newPath + ".xopp");
  plugin.app.fileManager.renameFile(pdfFile, newPath + ".pdf");
}
function deleteXoppAndPdf(plugin, xoppFile, pdfFile) {
  plugin.app.vault.delete(xoppFile);
  plugin.app.vault.delete(pdfFile);
}

// src/CreateXoppModalManager.ts
var import_obsidian8 = require("obsidian");

// src/modals/XoppFileNameModal.ts
var import_obsidian6 = require("obsidian");
var XoppFileNameModal = class extends import_obsidian6.Modal {
  constructor(app, createFile, createAndOpenFile) {
    super(app);
    this.createFile = createFile;
    this.createAndOpenFile = createAndOpenFile;
  }
  onOpen() {
    const { contentEl } = this;
    const container = contentEl.createDiv({ cls: "new-xopp-file-modal-form" });
    let fileName;
    const textComponent = new import_obsidian6.TextComponent(container).setPlaceholder("Enter a file name").onChange((i) => {
      fileName = i;
    });
    textComponent.inputEl.focus();
    textComponent.inputEl.addEventListener("keypress", (e) => {
      if (e.key === "Enter") {
        if (e.shiftKey) {
          this.createAndOpenFile(fileName);
          this.close();
        } else {
          this.createFile(fileName);
          this.close();
        }
      }
    });
    const buttonRow = container.createDiv({ cls: "button-row" });
    new import_obsidian6.ButtonComponent(buttonRow).setButtonText("Create").setTooltip("Enter").onClick(() => {
      this.createFile(fileName);
      this.close();
    });
    new import_obsidian6.ButtonComponent(buttonRow).setButtonText("Create & Open").setTooltip("Shift + Enter").onClick(() => {
      this.createAndOpenFile(fileName);
      this.close();
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/modals/FolderSuggestModal.ts
var import_obsidian7 = require("obsidian");
var FolderSuggestModal = class extends import_obsidian7.FuzzySuggestModal {
  constructor(app, plugin, onCloseFolderModal) {
    super(app);
    this.plugin = plugin;
    this.onCloseModal = onCloseFolderModal;
  }
  getItems() {
    let folders = [];
    const folderPaths = /* @__PURE__ */ new Set();
    this.app.vault.getAllLoadedFiles().forEach((file) => {
      if (file instanceof import_obsidian7.TFolder) {
        const normalizedPath = file.path.startsWith("/") ? file.path.slice(1) : file.path;
        if (!folderPaths.has(normalizedPath)) {
          folders.push(file);
          folderPaths.add(normalizedPath);
        }
      }
    });
    let defaultFolderPath = this.plugin.settings.defaultNewFilePath;
    if (defaultFolderPath.endsWith("/")) {
      defaultFolderPath = defaultFolderPath.slice(0, -1);
    }
    let defaultFolder = this.app.vault.getFolderByPath(defaultFolderPath);
    if (defaultFolder != null && this.inputEl.value == "")
      folders = [defaultFolder].concat(folders);
    return folders;
  }
  getItemText(folder) {
    return folder.path;
  }
  onOpen() {
    super.onOpen();
    this.inputEl.addEventListener("keydown", (event) => {
      if (event.key === "Tab") {
        event.preventDefault();
        let selectedElement = this.modalEl.getElementsByClassName("is-selected").item(0);
        let selected = selectedElement ? selectedElement.textContent : "";
        this.inputEl.value = selected || "";
        this.setContent("test");
        this.inputEl.dispatchEvent(new Event("input"));
      }
    });
  }
  onChooseItem(folder) {
    this.onCloseModal(folder.path);
  }
};

// src/CreateXoppModalManager.ts
var CreateXoppModalManager = class {
  constructor(app, plugin, filePath = "", editor = null, linksToInsert = "") {
    this.openAfterCreate = false;
    this.app = app;
    this.plugin = plugin;
    this.filePath = filePath;
    this.editor = editor;
    this.linksToInsert = linksToInsert;
    this.createModals();
  }
  createModals() {
    const onCloseFolderModal = (folderPath) => new XoppFileNameModal(
      this.plugin.app,
      (fileName) => this.onCreate(folderPath, fileName),
      (fileName) => this.onCreateAndOpen(folderPath, fileName)
    ).setTitle("Create a new Xournal++ note").open();
    const folderSuggestModal = new FolderSuggestModal(this.app, this.plugin, onCloseFolderModal);
    folderSuggestModal.setPlaceholder("Select a folder for the new Xournal++ note");
    folderSuggestModal.setInstructions([
      { command: "Arrows", purpose: "to navigate" },
      { command: "Tab", purpose: "to autocomplete folder" },
      { command: "Enter", purpose: "to select folder" }
    ]);
    if (this.filePath == "") {
      folderSuggestModal.open();
    } else {
      onCloseFolderModal(this.filePath);
    }
  }
  async onCreate(folderPath, fileName) {
    fileName += ".xopp";
    await createXoppFile(this.plugin, folderPath === "/" ? fileName : `${folderPath}/${fileName}`);
    if (this.editor instanceof import_obsidian8.Editor)
      this.insertLink(this.editor, folderPath, fileName, this.linksToInsert);
  }
  async onCreateAndOpen(folderPath, fileName) {
    await this.onCreate(folderPath, fileName);
    const filePath = folderPath === "/" ? fileName : `${folderPath}/${fileName}`;
    const file = await this.waitForFileToBeIndexed(filePath + ".xopp");
    if (file) {
      openXournalppFile(file, this.plugin);
    } else {
      console.error("Failed to open the file after creation.");
      new import_obsidian8.Notice("Failed to open the file after creation.");
    }
  }
  insertLink(editor, filePath, fileName, linksToInsert) {
    if (filePath !== "/")
      filePath = filePath + "/";
    fileName = fileName.replace(".xopp", "");
    let xoppLink = "[[" + filePath + fileName + ".xopp|" + fileName + "]]";
    let pdfLink = "[[" + filePath + fileName + ".pdf|" + fileName + "]]";
    let finalLink = xoppLink + " " + pdfLink;
    if (linksToInsert === "embeddedPDF")
      finalLink = "!" + pdfLink;
    if (linksToInsert === "PDF")
      finalLink = pdfLink;
    if (linksToInsert === "XOPP")
      finalLink = xoppLink;
    editor.replaceRange(finalLink, editor.getCursor());
  }
  async waitForFileToBeIndexed(path, timeout = 5e3) {
    const interval = 100;
    const maxAttempts = timeout / interval;
    for (let attempt = 0; attempt < maxAttempts; attempt++) {
      const file = this.plugin.app.vault.getFileByPath(path);
      if (file)
        return file;
      await new Promise((resolve) => setTimeout(resolve, interval));
    }
    return null;
  }
};

// src/modals/RenameModal.ts
var import_obsidian9 = require("obsidian");
var RenameModal = class extends import_obsidian9.Modal {
  constructor(app, filePath, renameFile) {
    super(app);
    this.filePath = filePath;
    this.renameFile = renameFile;
  }
  onOpen() {
    const { contentEl } = this;
    const container = contentEl.createDiv({ cls: "new-file-modal-form" });
    let fileName;
    let prev = this.filePath.split("/");
    let fullFileName = prev[prev.length - 1];
    fileName = fullFileName.split(".")[0];
    const textComponent = new import_obsidian9.TextComponent(container).setPlaceholder("Enter a new file name").setValue(fileName).onChange((i) => {
      fileName = i;
    });
    textComponent.inputEl.focus();
    textComponent.inputEl.addEventListener("keypress", (e) => {
      if (e.key === "Enter") {
        this.renameFile(fileName);
        this.close();
      }
    });
    new import_obsidian9.ButtonComponent(container).setButtonText("Create").onClick(() => {
      this.renameFile(fileName);
      this.close();
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/modals/SearchXoppModal.ts
var import_obsidian10 = require("obsidian");
var SearchXoppModal = class extends import_obsidian10.FuzzySuggestModal {
  constructor(app, plugin) {
    super(app);
    this.plugin = plugin;
  }
  getItems() {
    let files = [];
    const filePaths = /* @__PURE__ */ new Set();
    this.app.vault.getAllLoadedFiles().forEach((file) => {
      if (file instanceof import_obsidian10.TFile && file.extension === "xopp") {
        const normalizedPath = file.path.startsWith("/") ? file.path.slice(1) : file.path;
        if (!filePaths.has(normalizedPath)) {
          files.push(file);
          filePaths.add(normalizedPath);
        }
      }
    });
    return files;
  }
  getItemText(file) {
    return file.path;
  }
  onOpen() {
    super.onOpen();
    this.inputEl.addEventListener("keydown", (event) => {
      if (event.key === "Tab") {
        event.preventDefault();
        let selectedElement = this.modalEl.getElementsByClassName("is-selected").item(0);
        let selected = selectedElement ? selectedElement.textContent : "";
        this.inputEl.value = selected || "";
        this.inputEl.dispatchEvent(new Event("input"));
      }
      if (event.key === "Enter") {
        event.preventDefault();
        if (event.shiftKey) {
          this.selectActiveSuggestion(event);
        } else if (event.ctrlKey || event.metaKey) {
          this.selectActiveSuggestion(event);
        }
      }
    });
  }
  onChooseItem(file, event) {
    if (event.shiftKey) {
      openXournalppFile(file, this.plugin);
      this.close();
    } else {
      const pdfFilePath = file.path.replace(".xopp", ".pdf");
      const pdfFile = this.plugin.app.vault.getFileByPath(pdfFilePath);
      if (!pdfFile) {
        new import_obsidian10.Notice("No corresponding PDF file found.");
        return;
      }
      this.plugin.app.workspace.getLeaf(event.ctrlKey || event.metaKey ? "split" : void 0, "vertical").openFile(pdfFile);
      this.close();
    }
  }
};

// src/commands.ts
function createCommands(plugin) {
  plugin.addCommand({
    id: "open-in-xournalpp",
    name: "Open current note",
    checkCallback: (checking) => {
      var _a, _b;
      let pdfFilePath = (_a = plugin.app.workspace.getActiveFile()) == null ? void 0 : _a.path;
      if (!pdfFilePath || ((_b = plugin.app.workspace.getActiveFile()) == null ? void 0 : _b.extension) !== "pdf")
        return false;
      let xoppFile = findCorrespondingXoppToPdf(pdfFilePath, plugin);
      if (!xoppFile)
        return false;
      if (!checking)
        openXournalppFile(xoppFile, plugin);
      return true;
    }
  });
  plugin.addCommand({
    id: "crate-new-xournalpp",
    name: "Create a new note",
    callback: async () => {
      new CreateXoppModalManager(plugin.app, plugin, "");
    }
  });
  plugin.addCommand({
    id: "crate-new-xournalpp-and-link-xopp",
    name: "Create a new note and insert XOPP link",
    editorCallback: async (editor) => {
      new CreateXoppModalManager(plugin.app, plugin, "", editor, "XOPP");
    }
  });
  plugin.addCommand({
    id: "crate-new-xournalpp-and-link-pdf",
    name: "Create a new note and insert PDF link",
    editorCallback: async (editor) => {
      new CreateXoppModalManager(plugin.app, plugin, "", editor, "PDF");
    }
  });
  plugin.addCommand({
    id: "crate-new-xournalpp-and-link-embedded-pdf",
    name: "Create a new note and insert embedded PDF link",
    editorCallback: async (editor) => {
      new CreateXoppModalManager(plugin.app, plugin, "", editor, "embeddedPDF");
    }
  });
  plugin.addCommand({
    id: "crate-new-xournalpp-and-link-xopp-pdf",
    name: "Create a new note and insert PDF and XOPP link",
    editorCallback: async (editor) => {
      new CreateXoppModalManager(plugin.app, plugin, "", editor);
    }
  });
  plugin.addCommand({
    id: "export-xournalpp-to-pdf",
    name: "Export all notes to PDF",
    callback: async () => {
      exportAllXoppToPDF(plugin);
    }
  });
  plugin.addCommand({
    id: "export-current-xournalpp-to-pdf",
    name: "Update current PDF",
    checkCallback: (checking) => {
      var _a;
      let filePath = (_a = plugin.app.workspace.getActiveFile()) == null ? void 0 : _a.path;
      if (filePath && filePath.includes(".pdf")) {
        if (!checking) {
          filePath = filePath == null ? void 0 : filePath.replace(".pdf", ".xopp");
          exportXoppToPDF(plugin, [filePath]);
        }
        return true;
      }
      return false;
    }
  });
  plugin.addCommand({
    id: "rename-xournalpp",
    name: "Rename current PDF and corresponding Xournal++ note",
    checkCallback: (checking) => {
      let pdfFile = plugin.app.workspace.getActiveFile();
      if (!pdfFile || !pdfFile.name.endsWith(".pdf"))
        return false;
      let xoppFile = findCorrespondingXoppToPdf(pdfFile.path, plugin);
      if (!xoppFile)
        return false;
      if (!checking) {
        const renameFile = (fileName) => renameXoppFile(plugin, xoppFile, pdfFile, fileName);
        new RenameModal(plugin.app, xoppFile.path, renameFile).open();
      }
      return true;
    }
  });
  plugin.addCommand({
    id: "delete-xournalpp",
    name: "Delete current PDF and corresponding Xournal++ note",
    checkCallback: (checking) => {
      let pdfFile = plugin.app.workspace.getActiveFile();
      if (!pdfFile || !pdfFile.name.endsWith(".pdf"))
        return false;
      let xoppFile = findCorrespondingXoppToPdf(pdfFile.path, plugin);
      if (!xoppFile)
        return false;
      if (!checking) {
        deleteXoppAndPdf(plugin, xoppFile, pdfFile);
      }
      return true;
    }
  });
  plugin.addCommand({
    id: "xournalpp-search-and-open",
    name: "Search and open Xournal++ note",
    callback: () => {
      const searchModal = new SearchXoppModal(plugin.app, plugin);
      searchModal.setPlaceholder("Search for a Xournal++ note");
      searchModal.setInstructions([
        { command: "Arrows", purpose: "to navigate" },
        { command: "Tab", purpose: "to autocomplete" },
        { command: "Enter", purpose: "to open the corresponding PDF" },
        { command: "Shift + Enter", purpose: "to open in Xournal++" }
      ]);
      searchModal.open();
    }
  });
}

// src/fileMenu.ts
var import_obsidian11 = require("obsidian");
function addXournalppOptionsToFileMenu(menu, file, plugin) {
  if (file instanceof import_obsidian11.TFile) {
    if (file.extension === "xopp") {
      addOpenInXournalppMenu(menu, file, plugin);
    } else if (file.extension === "pdf") {
      let xoppFile = findCorrespondingXoppToPdf(file.path, plugin);
      if (xoppFile) {
        addOpenInXournalppMenu(menu, xoppFile, plugin);
        addXournalppRenameMenu(menu, file, xoppFile, plugin);
        addXournalppDeleteMenu(menu, file, xoppFile, plugin);
        removeDeleteRenameMenuItem();
      }
    }
  } else if (file instanceof import_obsidian11.TFolder) {
    if (file == null ? void 0 : file.children) {
      addCreateXournalppMenu(menu, file, plugin);
    }
  }
}
function addOpenInXournalppMenu(menu, xoppFile, plugin) {
  menu.addItem((item) => {
    item.setTitle("Open in Xournal++").setIcon("pen-tool").onClick(() => {
      openXournalppFile(xoppFile, plugin);
    });
  });
  menu.addItem((item) => {
    item.setTitle("Update from Xournal++").setIcon("rotate-cw").onClick(() => {
      var _a;
      let filePath = (_a = plugin.app.workspace.getActiveFile()) == null ? void 0 : _a.path;
      filePath = filePath == null ? void 0 : filePath.replace(".pdf", ".xopp");
      exportXoppToPDF(plugin, [filePath]);
    });
  });
}
function addCreateXournalppMenu(menu, folder, plugin) {
  menu.addItem((item) => {
    item.setTitle("Create new Xournal++").setIcon("pen-tool").onClick(() => {
      var _a;
      new CreateXoppModalManager(plugin.app, plugin, (_a = folder == null ? void 0 : folder.path) != null ? _a : "");
    });
  });
}
function addXournalppDeleteMenu(menu, pdfFile, xoppFile, plugin) {
  menu.addItem((item) => {
    item.setTitle("Delete PDF & Xournal++").setIcon("trash").setSection("danger").onClick(() => {
      deleteXoppAndPdf(plugin, xoppFile, pdfFile);
    });
  });
}
function addXournalppRenameMenu(menu, pdfFile, xoppFile, plugin) {
  menu.addItem((item) => {
    item.setTitle("Rename PDF & Xournal++...").setIcon("pencil").setSection("danger").onClick(() => {
      new RenameModal(plugin.app, xoppFile.path, renameFile).open();
    });
  });
  const renameFile = (fileName) => renameXoppFile(plugin, xoppFile, pdfFile, fileName);
}
function removeDeleteRenameMenuItem() {
  const body = document.querySelector("body");
  if (!body)
    return;
  const observer = new MutationObserver(() => {
    const menuItems = body.querySelectorAll(".menu-item");
    if (menuItems.length > 0) {
      let isXoppMenu = false;
      menuItems.forEach((item) => {
        if (item.textContent && item.textContent == "Delete PDF & Xournal++") {
          item.classList.add("is-warning");
          isXoppMenu = true;
        }
      });
      if (isXoppMenu) {
        menuItems.forEach((item) => {
          if (item.textContent && (item.textContent == "Delete" || item.textContent == "Rename..."))
            item.remove();
        });
      }
      observer.disconnect();
    }
  });
  observer.observe(body, {
    childList: true,
    subtree: true
  });
}

// src/pdfToolbar.ts
var import_obsidian12 = require("obsidian");
function addOpenInXournalppToPdfToolbar(file, plugin) {
  if (file && file.extension === "pdf") {
    const oldIcons = document.querySelectorAll(".xournalpp-open-icon");
    oldIcons.forEach((old) => old.remove());
    let xoppFile = findCorrespondingXoppToPdf(file.path, plugin);
    if (xoppFile) {
      const pdfToolbar = document.querySelector(".pdf-toolbar-right");
      if (pdfToolbar) {
        createPdfToolbarButton(pdfToolbar, xoppFile, plugin);
      }
    }
  }
}
function createPdfToolbarButton(pdfToolbar, xoppFile, plugin) {
  let xoppButton = new import_obsidian12.ButtonComponent(pdfToolbar).setClass("clickable-icon").setClass("xournalpp-open-icon").setButtonText("Edit in Xournal++").setIcon("pen-tool").setTooltip("Edit in Xournal++");
  xoppButton.onClick(() => {
    openXournalppFile(xoppFile, plugin);
  });
  pdfToolbar.appendChild(xoppButton.buttonEl);
}

// src/fileExplorerNav.ts
var import_obsidian13 = require("obsidian");
function addCreateXournalppNavIcon(plugin) {
  let fileExplorers = plugin.app.workspace.getLeavesOfType("file-explorer");
  fileExplorers.forEach((fileExplorer) => {
    const fileExplorerView = fileExplorer.view;
    const headerDom = fileExplorerView == null ? void 0 : fileExplorerView.headerDom;
    const navButtonsEl = headerDom == null ? void 0 : headerDom.navButtonsEl;
    if (navButtonsEl && navButtonsEl.children.length > 2 && !navButtonsEl.querySelector(".xournalpp-create-icon")) {
      let createXoppButton = new import_obsidian13.ButtonComponent(navButtonsEl).setClass("clickable-icon").setClass("xournalpp-create-icon").setIcon("pen-tool").setTooltip("Create new Xournal++");
      createXoppButton.onClick(() => {
        new CreateXoppModalManager(plugin.app, plugin);
      });
      navButtonsEl.insertAfter(createXoppButton.buttonEl, navButtonsEl.children[1]);
    }
  });
}

// src/fileExplorerFile.ts
function addOpenInXournalpp(plugin) {
  plugin.app.workspace.onLayoutReady(() => {
    observeFileExplorer(plugin);
    applyXournalppTags(plugin);
  });
}
function observeFileExplorer(plugin) {
  let fileExplorers = plugin.app.workspace.getLeavesOfType("file-explorer");
  fileExplorers.forEach((fileExplorer) => {
    var _a;
    let container = (_a = fileExplorer.view) == null ? void 0 : _a.containerEl;
    if (container) {
      const observer = new MutationObserver(() => {
        applyXournalppTags(plugin);
      });
      observer.observe(container, { childList: true, subtree: true });
    }
  });
}
function applyXournalppTags(plugin) {
  let fileExplorers = plugin.app.workspace.getLeavesOfType("file-explorer");
  fileExplorers.forEach((fileExplorer) => {
    var _a;
    let allFiles = (_a = fileExplorer.view) == null ? void 0 : _a.fileItems;
    if (!allFiles)
      return;
    Object.entries(allFiles).forEach(([filePath, value]) => {
      if (filePath.endsWith(".pdf")) {
        let xoppFile = findCorrespondingXoppToPdf(filePath, plugin);
        if (xoppFile && (value == null ? void 0 : value.tagEl)) {
          const tagEl = value.tagEl;
          if (tagEl && (tagEl.innerText != "X++" || !tagEl.classList.contains("clickable-tag"))) {
            tagEl.innerText = "X++";
            tagEl.classList.add("clickable-tag");
            tagEl.onclick = () => {
              openXournalppFile(xoppFile, plugin);
            };
          }
        }
      }
    });
  });
}

// src/listeners.ts
function setupListeners(plugin) {
  plugin.registerEvent(plugin.app.workspace.on("layout-change", () => {
    addCreateXournalppNavIcon(plugin);
    addOpenInXournalpp(plugin);
  }));
  plugin.registerEvent(plugin.app.workspace.on("file-open", (file) => {
    addOpenInXournalppToPdfToolbar(file, plugin);
  }));
  plugin.registerEvent(plugin.app.workspace.on("file-menu", (menu, file) => {
    addXournalppOptionsToFileMenu(menu, file, plugin);
  }));
  plugin.registerEvent(plugin.app.workspace.on("active-leaf-change", (leaf) => {
    if ((leaf == null ? void 0 : leaf.getDisplayText()) === "Files") {
      addCreateXournalppNavIcon(plugin);
      addOpenInXournalpp(plugin);
    }
  }));
  plugin.registerEvent(plugin.app.vault.on("modify", (file) => {
    if (file.extension === "xopp" && plugin.settings.autoExport)
      exportXoppToPDF(plugin, [file.path], false);
  }));
  plugin.app.workspace.onLayoutReady(() => {
    initialLoad(plugin);
    plugin.registerEvent(plugin.app.vault.on("create", (file) => {
      if (file.extension === "xopp" && plugin.settings.autoExport)
        exportXoppToPDF(plugin, [file.path], false);
    }));
  });
}
function initialLoad(plugin) {
  if (plugin.settings.autoExport) {
    let files = plugin.app.vault.getFiles();
    let xopp_files = files.filter((file) => file.extension === "xopp");
    let pdf_files = files.filter((file) => file.extension === "pdf");
    let filePaths = [];
    for (const xopp_file of xopp_files) {
      let pdf_file = pdf_files.find((pdf) => pdf.path === xopp_file.path.replace(".xopp", ".pdf")) || false;
      let xopp_is_newer = false;
      if (pdf_file) {
        xopp_is_newer = xopp_file.stat.mtime > pdf_file.stat.mtime;
      }
      if (!pdf_file || xopp_is_newer) {
        filePaths.push(xopp_file.path);
      }
    }
    if (filePaths.length > 0) {
      exportXoppToPDF(plugin, filePaths, false);
    }
  }
  addCreateXournalppNavIcon(plugin);
  addOpenInXournalpp(plugin);
}

// src/ribbonIcons.ts
function createRibbonIcons(plugin) {
  plugin.addRibbonIcon("pen-tool", "Create new Xournal++ note", (evt) => {
    new CreateXoppModalManager(plugin.app, plugin);
  });
}

// main.ts
var DEFAULT_SETTINGS = {
  autoExport: false,
  xournalppPath: "",
  templatePath: "",
  defaultNewFilePath: ""
};
var XoppPlugin = class extends import_obsidian14.Plugin {
  async onload() {
    await this.loadSettings();
    this.addSettingTab(new XoppSettingsTab(this.app, this));
    setupListeners(this);
    createCommands(this);
    createRibbonIcons(this);
    checkXoppSetup(this);
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
  onunload() {
  }
};


/* nosourcemap */