1. 简谐运动：由于满足 $F = m a = -k x$，则化为 $frac(dif^2 x, dif t^2) + k/m x = 0$，设 $omega^2 = k/m$，则原式化为 $dot.double(x) + omega^2 x = 0$ .

2. 对于存在阻尼的 SHM，$m dot.double(x) = -k x - b dot(x)$，同样令 $omega^2 = k/m, gamma = b/m$，则其满足 $dot.double(x) + gamma dot(x) + omega x = 0$，